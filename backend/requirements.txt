# Core FastAPI
fastapi>=0.100.0
uvicorn>=0.20.0
mangum>=0.17.0
python-multipart>=0.0.5

# Database (SQLite is built-in, just need SQLAlchemy)
sqlmodel>=0.0.12
SQLAlchemy>=2.0.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt<5.0  # Pin to <5.0 for passlib compatibility

# AWS
boto3>=1.28.0

# Utilities
python-dotenv>=1.0.0

# Pagination
fastapi-pagination>=0.12.0
